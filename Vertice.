#include <iostream>
using namespace std;

class Vertice {
    private:
    Vertice id;
    Vertice dato;
    public:
      Vertice(Vertice d, Vertice ato){
        id= d;
        dato= ato;
      }
      Vertice getId(){
        return id;
      }
      Vertice getDato(){
       return dato;
      }
      void setId(Vertice i){
       id = i;
      }
      void setDato(Vertice d){
       dato = d;
      }
 Return 0;
}
  
